import { Component } from '@angular/core';
import { Routes } from '@angular/router';
import { AllBooksComponent } from './all-books/all-books.component';

export const routes: Routes = [{path:'books',component:AllBooksComponent}];
